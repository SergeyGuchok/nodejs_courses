class Index {
    constructor(params) {
        this.id = params.id
        this.login = params.login
        this.password = params.password
        this.age = params.age
        this.isDeleted = false
    }
}

export default Index
